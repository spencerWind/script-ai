import PropTypes from 'prop-types'


const UpdateBudgetProgress = ({budget}) => {
  return (
      <div
          id="updateBudgetProgressForm"
          className="">
          UpdateBudgetProgress
      </div>
  );
}

UpdateBudgetProgress.propTypes = {
    budget: PropTypes.any
}

export default UpdateBudgetProgress